import styled from 'styled-components';

export const StyledName = styled.span`
  font-weight: 700;
`;

export const StyledAssets = styled.span`
  opacity: 0.7;
  font-size: 12px;
`;

export const StyledTitle = styled.div`
  display: grid;
  grid-gap: 8px;
`;

export const StyledState = styled.div`
  display: grid;
  justify-items: end;
  align-items: start;
  overflow: hidden;
`;

export const StyledDates = styled.div`
  display: grid;
  justify-content: start;
  justify-items: end;
  align-items: center;
`;

export const StyledDate = styled.span`
  margin-right: 2px;
  opacity: 0.7;
  font-size: 11px;
`;
